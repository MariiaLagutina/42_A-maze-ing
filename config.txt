# ==============================
# A-Maze-Ing Configuration File
# ==============================

# Maze dimensions
# Must be positive integers
WIDTH = 20
HEIGHT = 20

# Entry point (x, y)
# Coordinates are 0-indexed
# Default: (0, 0)
ENTRY = 0,0

# Exit point (x, y)
# Default: (WIDTH - 1, HEIGHT - 1)
EXIT = 19,19

# Maze generation algorithm
# Options:
#   - backtracker  (Recursive Backtracker / DFS)
#   - wilson       (Wilson's Algorithm)
ALGO = wilson

# Perfect maze flag
# true  -> perfect maze (no loops)
# false -> imperfect maze (loops allowed)
PERFECT = true

# Output file name
OUTPUT_FILE = maze.txt

# Random seed (optional)
# If omitted, randomness is not reproducible
# SEED = 42
